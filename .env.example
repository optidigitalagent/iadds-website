SITE_URL=http://127.0.0.1:3100
CONSULTATION_WEBHOOK_URL=
CONSULTATION_WEBHOOK_SECRET=
ANALYTICS_ID=
# Only when a trusted proxy overwrites X-Forwarded-For.
TRUST_PROXY=false
# Development storage only; production always requires the persistent provider.
CONSULTATION_LOCAL_DIR=consultations
